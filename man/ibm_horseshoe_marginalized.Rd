% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibm_horseshoe_marginalized.R
\name{ibm_horseshoe_marginalized}
\alias{ibm_horseshoe_marginalized}
\title{Fit a marginalized Gaussian horseshoe IBM}
\usage{
ibm_horseshoe_marginalized(
  t,
  y,
  infer_at = NULL,
  log_sigma = list(mu = -1, sd = 1),
  zeta = 0.1,
  initial_sd = 5,
  n_state_draws = 1000L,
  seed = NULL,
  iter = 2000,
  chains = 4,
  cores = getOption("mc.cores", chains),
  max_treedepth = 12,
  adapt_delta = 0.9,
  ...
)
}
\arguments{
\item{t}{Numeric vector of observation times.}

\item{y}{Numeric vector of observations.}

\item{infer_at}{Optional additional times at which state draws are required.}

\item{log_sigma}{Prior specification used by the existing Stan backend.}

\item{zeta}{Global horseshoe scale.}

\item{initial_sd}{Scale of the correlated unit-time IBM initial-state prior.}

\item{n_state_draws}{Number of conditional state trajectories to reconstruct.
Set to zero to retain only hyperparameter draws.}

\item{seed}{Optional random seed used by Stan and the simulation smoother.}

\item{iter, chains, cores}{Stan sampling controls.}

\item{max_treedepth, adapt_delta}{Stan HMC controls.}

\item{...}{Additional arguments passed to \code{\link[rstan:stan]{rstan::stan()}}.}
}
\value{
An object of class \code{ibmfit} containing the marginalized Stan fit and
cached posterior state draws.
}
\description{
Fits the same Gaussian-data model as the current noncentered horseshoe Stan
model, but integrates the two-dimensional IBM state out with a Kalman filter
during HMC. Exact conditional state trajectories are reconstructed afterward
with a simulation smoother.
}
\details{
The model is also available through \code{\link[=ibm]{ibm()}} by selecting the marginalized
horseshoe engine.
}
