% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_curve}
\alias{predict_curve}
\title{Predict an IBM curve after fitting}
\usage{
predict_curve(ibmfit, new_t = NULL, n_samples = 1000, seed = NULL)
}
\arguments{
\item{ibmfit}{An \code{ibmfit}.}

\item{new_t}{Finite numeric prediction locations. The default uses the
observed locations together with the fit's original \code{infer_at}.}

\item{n_samples}{Maximum number of posterior draws.}

\item{seed}{Optional random seed for bridge and extrapolation draws.}
}
\value{
A list with \code{t}, \code{f}, and \code{fprime}. Draws are rows and locations are
columns.
}
\description{
Draws function values and derivatives at arbitrary locations without adding
those locations to the Stan latent state. Within the observed range, draws
use the exact IBM bridge conditional on adjacent sampled states. Outside the
range they use forward or reverse IBM transitions.
}
