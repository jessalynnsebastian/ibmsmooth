% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocess.R
\name{summarize_performance}
\alias{summarize_performance}
\title{Summarize function and derivative estimation performance}
\usage{
summarize_performance(
  ibmfit,
  truth,
  new_t = NULL,
  level = 0.95,
  n_samples = 1000,
  seed = NULL,
  pointwise = FALSE
)
}
\arguments{
\item{ibmfit}{An \code{ibmfit} object.}

\item{truth}{Named list containing \code{f} and/or \code{fprime}. Each element can be a
numeric vector evaluated at \code{new_t} or a function that accepts \code{new_t}.}

\item{new_t}{Optional finite evaluation locations. By default, uses the
fitted prediction grid.}

\item{level}{Credible interval confidence level strictly between zero and
one.}

\item{n_samples}{Maximum number of posterior draws.}

\item{seed}{Optional seed for conditional bridge draws at new locations.}

\item{pointwise}{If \code{TRUE}, return both aggregate and location-specific
results; otherwise return only the aggregate data frame.}
}
\value{
A data frame with one row per supplied truth, or a list containing
\code{aggregate} and \code{pointwise} data frames when \code{pointwise = TRUE}.
}
\description{
Computes mean absolute deviation (MAD) of the posterior median, mean
credible interval width (MCIW), empirical coverage percentage, and the
empirical continuous ranked probability score (CRPS), pointwise and averaged
across evaluation locations.
}
