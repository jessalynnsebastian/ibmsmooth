% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibm_inla.R
\name{ibm_inla_fit}
\alias{ibm_inla_fit}
\title{Fit an integrated Brownian motion smoother using INLA}
\usage{
ibm_inla_fit(
  t,
  y,
  infer_at = NULL,
  adaptive = FALSE,
  n_knots = 5L,
  bs_degree = 3L,
  adaptive_deviation_sd = 0.5,
  log_process_sd = list(mu = -2, sd = 0.5),
  log_sigma = list(mu = -1, sd = 1),
  theta_mu = NULL,
  theta_sd = NULL,
  sigma_mu = NULL,
  sigma_sd = NULL,
  initial_sd = 5,
  initial_slope_sd = NULL,
  control.compute = list(config = TRUE),
  ...
)
}
\arguments{
\item{t}{Numeric vector of time points.}

\item{y}{Numeric vector of observations at times \code{t}.}

\item{infer_at}{Optional numeric vector of additional time points at which
inference should be returned.}

\item{adaptive}{Logical. If \code{FALSE}, use one global diffusion-precision parameter. If
\code{TRUE}, model the log diffusion precision as a global log precision plus
centered B-spline deviations shrunk toward zero.}

\item{n_knots}{Number of internal knots for the B-spline basis when
\code{adaptive = TRUE}.}

\item{bs_degree}{Degree of the B-spline basis.}

\item{adaptive_deviation_sd}{Prior standard deviation for the centered
B-spline deviation coefficients in adaptive INLA fits. The adaptive model
uses \verb{log(lambda_i) = alpha + B_i beta}, where \code{alpha} is the global log
diffusion precision and the components of \code{beta} have independent
\code{N(0, adaptive_deviation_sd^2)} priors. Smaller values shrink the model
more strongly toward the global-precision IBM.}

\item{log_process_sd}{List with elements \code{mu} and \code{sd} giving the prior
mean and standard deviation for the log IBM process standard deviation on
the scaled model scale. The default matches the Stan backend:
\code{list(mu = -2, sd = 0.5)}.}

\item{log_sigma}{List with elements \code{mu} and \code{sd} giving the prior mean and
standard deviation for the log Gaussian observation standard deviation on
the scaled model scale. The default matches the Stan backend:
\code{list(mu = -1, sd = 1)}.}

\item{theta_mu}{Optional advanced override for the mean of the internal INLA
global log diffusion-precision parameter. If \code{NULL}, this is set from
\code{log_process_sd}.}

\item{theta_sd}{Optional advanced override for the standard deviation of the
internal INLA global log diffusion-precision parameter. If \code{NULL}, this is
set from \code{log_process_sd}.}

\item{sigma_mu}{Optional advanced override for the mean of the Gaussian
likelihood log precision. If \code{NULL}, this is set from \code{log_sigma}.}

\item{sigma_sd}{Optional advanced override for the standard deviation of the
Gaussian likelihood log precision. If \code{NULL}, this is set from \code{log_sigma}.}

\item{initial_sd}{Prior scale for the joint initial state \code{c(f_1, fprime_1)}.
The initial covariance is \code{initial_sd^2 * matrix(c(1/3, 1/2, 1/2, 1), 2, 2)},
the unit-time integrated Brownian motion covariance for the level and derivative.}

\item{initial_slope_sd}{Deprecated. Retained only for backward compatibility;
ignored by the INLA backend.}

\item{control.compute}{List passed to \code{INLA::inla()} as \code{control.compute}.
Defaults to \code{list(config = TRUE)} so that posterior sampling works for
plotting, summaries, and \code{get_samples_inla()}.}

\item{...}{Additional arguments passed to \code{INLA::inla()}.}
}
\value{
An object of class \code{ibmfit}.
}
\description{
Fit a global or locally adaptive integrated Brownian motion smoother using
INLA. The latent field is the state vector \verb{(f, fprime)} on the latent time
grid. The observation model is Gaussian.
}
